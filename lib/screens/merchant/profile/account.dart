// Thin redirect — the real merchant account screen is AccountScreen(role: 'Merchant').
// This file is kept so that any future import of profile/account.dart
// resolves without error, while all implementation lives in the shared AccountScreen.
export '../../account/account_screen.dart';
